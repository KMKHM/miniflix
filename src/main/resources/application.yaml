spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miniflix
    username: miniflix
    password: miniflix123
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret: miniflix-secret-key-must-be-at-least-32-characters-long
  expiration: 86400000  # 24시간